# Часть 1. Трассировка выполнения (самое важное для закрепления)
# Для каждого случая распиши по шагам, что произойдёт: какие методы вызовутся, в каком порядке,
# что вернётся или какая ошибка вылетит.
# 1.1. Клиент отправляет POST с телом: {"rating": "***", "text": "Отличный табак"}


# в класс RatingField прийдёт значение rating то есть три звёздочки это будет data потом мы проверяем
# если data это строка и символ звёздочки в data то тогда мы считаем количество элементов  в строке и получаем число
# и присваивыаем это преременной rating потом мы дате присваивыаем rating потом мы определяем переменную value и
# в ней вызываем базовый метод класса IntegerField super().to_internal_value(data)
# или же IntegerField.to_internal_value(data) c аргументом data или же просто число 3 и там оно
# валидируеться например: max_value, min_value итд... и после всего этого в value остаёться число 3 и потом
# идёт проверка если value меньше одного или value больше пяти то тогда мы  выбрасываем ошыбку что у нас не правельный рейтинг
# а если всё ок тогда возвращаем цифру 3


# 1.2. Клиент отправляет:
# json{"rating": 7, "text": "..."}

# в метод def to_internal_value(self, data) прийдёт число 7 и потом оно будет проверяться если это строка  эсли это
# не строка тогда мы в values вызываем super().to_internal_value(data) на месте data будет 7 оно там валидируеться
# и если всё ок в values будет число 7 дальше оно проходит проверку если 7 меньше 1 или 7 больше 5 тогда выбрасываем ошыбку
# и оно и выбрасит ошыбку с сообщением 'Rating must be between 1 and 5.'



# 1.3. Пользователь уже оставлял отзыв на этот табак, и присылает ещё один POST запрос.
# Опиши: ReviewSerializer.to_internal_value вызовется до или после того, как RatingField.to_internal_value отработает для поля
# rating? Почему это важно (подумай, в каком порядке DRF вообще вызывает вложенные to_internal_value)?


# В метод def to_internal_value(self, data) в ReviewSerializer приходит то что клиент отправляет на сервер POST-запрос с минимальным JSON, так как об авторе и табаке сервер уже знает из контекста дальше
#Отрабатывает super().to_internal_value(data)Стандартный механизм DRF берет эту строку/числа и проверяет их:
# Проверяет, что rating соответствует правилам RatingField.Проверяет, что text — это строка, и она не пустая.Игнорирует поля author_email и author_full_name, так как они read_only=True.Переменная internal_data теперь содержит чистые данные Python
# Извлечение контекста и объектов БДМетод достает из контекста request и tobacco_id = 7:
# Проверка на дубликат (Валидация)Выполняется запрос в базу данных:Review.objects.filter(tobacco=tobacco, author_name=user).exists()
# Вариант А (Уже есть отзыв): Если Иван уже писал отзыв на "Overdose", сервер вернет клиенту ошибку 400 Bad Request: {"non_field_errors": ["You have already reviewed this tobacco."]}. Код дальше не пойдет.Вариант Б (Отзыва еще нет): Проверка пройдена, идем дальше.
# Обогащение данныхВ словарь internal_data принудительно вшиваются полноценные Django-объекты из базы данных:
# Что происходит после return internal_dataЭтот наполненный словарь возвращается из метода. DRF передает его в метод create(), который под капотом делает эквивалент следующего SQL-запроса
























